import {OrganizationForm} from "@/components/auth/organization-form.tsx";import {getMemberships,requireUser} from "@/lib/auth/session.ts";
export default async function Page(){const user=await requireUser(),memberships=await getMemberships(user.id);return <main className="onboarding"><div className="steps"><b>1</b><i/><b className="active">2</b><i/><b>3</b></div><section><p className="eyebrow">{memberships.length?"منشأة إضافية":"إعداد المنشأة"}</p><h1>{memberships.length?"أضف محفظة مستقلة":"ما الاسم الذي تريد أن تظهر به محفظتك؟"}</h1><p>ستُضاف تلقائيًا كمالك للمنشأة، وتبقى بياناتها مستقلة عن أي منشأة أخرى.</p><OrganizationForm/><aside>✓ إنشاء المنشأة وعضوية المالك يحدثان في عملية واحدة؛ لن تُترك محفظة بلا مالك.</aside></section></main>}
